platform :ios, '9.1'

target "Help A Paw" do
    
    # Don't show warnings from pods
    inhibit_all_warnings!
    
	pod 'Backendless', '~> 4.0'
	pod "ViewDeck"
	pod 'Fabric'
	pod 'Crashlytics'
    pod 'SDWebImage'
end
